# Design QA

## 目标

把网站从“研究成果展板”改为“全栈工作台地图”。用户无需先理解 M/A/G，就能从开发阶段进入具体节点，并在首屏看见任务、交付结果和 3 个候选。

本轮没有可用的新 ImageGen 视觉稿：生成请求因额度限制返回 429，用户已明确允许按文字方向直接实施。因此本轮以“暖粉色、少装饰卡片、主路径优先、保留节点—角色—关系—证据”作为视觉真值，并用改版前后浏览器截图做结构与可用性对照；不把旧截图冒充新设计稿。

## 验收记录

### Round 1

- P1：实现仍有一段巨型价值标题，工作台被压到第二屏。已把价值说明收进左栏，让阶段轨道和当前工作成为首要内容。
- P1：默认“写代码、跑测试”显示 YCloud、Ansight、Nightshift，虽然符合原始分数，但不够通用。已增加显式编辑层；完整排名不变，工作台结合通用性和当前安装状态选 3 个。
- P1：节点短句仍出现“独立证明它没有骗你”等生成式口号。已为 14 个节点重写自然语言标题、说明和三项任务。
- P2：卡片和证据文字偏小。已提高任务、候选说明、状态和依据文字字号。

### Round 2

- 桌面主路径通过：五阶段切换、节点页签、框架编号开关、任务勾选、推荐依据展开、节点目录跳转。
- 检索路径通过：全局搜索可带查询进入完整目录，筛选结果与详情弹窗可打开和关闭。
- 390 × 844 窄屏通过：导航换行、阶段横向滚动、节点页签横向滚动、任务单列，未出现横向撑破主内容。
- 参考图中的装饰性阶段图标没有照搬；实现使用数字阶段标记，避免伪造图标或引入外部运行资源。信息层级、布局、色彩和交互关系保持一致。

### Round 3 — 双工作台

- 桌面 1910 × 1075 通过：`Codex 插件 / Agent 技能` 切换保持原有信息层级；Agent Skill 行清楚显示父仓库、能力形态、静态证据状态与三件事。
- 完整目录通过：四类对象可独立切换；40 条精选、81 项项目审计、搜索、父项目跳转和冻结来源详情均可用。外部对象不显示虚假的 Codex 安装状态。
- P1：新增切换器与阶段页签让窄屏 grid 采用子元素最小内容宽度，整页出现横向溢出。已将移动端 track 改为 `minmax(0, 1fr)`，并给 rail、panel 和横向滚动容器补齐 `min-width: 0`。
- 390 × 844 CSS 视口复核通过：document `scrollWidth === innerWidth`；阶段与节点列表只在自身容器内横向滚动；双工作台切换器保持首屏可见；详情 dialog 宽 352px、内部无横向溢出。
- 浏览器控制台 `warning/error` 为 0。

### Round 4 — Benchmark 校准披露

- 桌面 1910 CSS px 复核通过：工作台、完整目录、研究方法三条主路径可用，页面 `scrollWidth === innerWidth`。
- 4 条校准候选均可从 Agent Skill 目录检索并打开详情；每条都显示 baseline / Skill 诊断分、固定来源和“评分器未通过，不能排名或晋级”的边界。
- 研究方法页明确披露 4 个候选、16 次运行、执行器通过、评分器失败和 0 条晋级，没有把校准 delta 写成效果排行。
- 约 390 CSS px 窄屏复核通过：document 无横向溢出；Agent Skill 切换可用；详情 dialog 宽约 352px，左右边界均在视口内，关闭按钮可见。
- 浏览器控制台 `warning/error` 为 0。

### Round 5 — 四角色能力工作包

- 桌面 1910 × 1075 通过：Agent 侧不再显示一张 Top 3 表，而是在同一节点并列展示主执行、Review、可视化、总结交接；每张卡只保留能力、证据入口、仓库、角色名次与覆盖方式。
- M06 复核通过：Review 有截图盲审与 Web 应用实测；可视化有前端设计、预览和截图差异；总结明确标为“通用补位”，没有冒充专用 Skill。
- M13 复核通过：GraphRAG Workbench 显示“来源已冻结 / 待封装”，open-slide 显示“通用补位”，并保留“缺少原生 Skill 谱系图”的缺口说明。
- 完整目录联合筛选通过：`M13 + 可视化` 只返回同一 workbench assignment 下的 GraphRAG Workbench 与 open-slide，不按研究主节点和角色做错误笛卡尔匹配。
- 跨节点详情上下文通过：从 `M13 · 可视化` 打开 GraphRAG Workbench 后，标题、badge、当前工作位与节点原文都保持 M13；刷新带 assignment 的详情深链仍保持 M13，研究主节点另列，不再偷换当前任务语境。
- wrapper 详情通过：清楚显示工作位分配、能力形态、许可证、证据状态、`candidate-only` 结论上限、依赖与未验证项。
- 证据链复核通过：artifact verifier 重算 116 条分配与 14 × 4 覆盖矩阵，并将页面内 `v1-40` 队列逐行、逐字段绑定到冻结 candidate plan。
- 窄屏断点通过：`scrollWidth === innerWidth`，四角色从双列降为单列，阶段和节点页签仍在自身容器内滚动。
- 深色模式通过：背景、任务卡、角色卡、状态 badge、缺口提示与链接保持可读；页面无横向溢出。
- 浏览器控制台 `warning/error` 为 0。

### Round 6 — 工作流关系扩展

- 研究方法页通过：关系区渲染完整 `WR01`–`WR14` 共 14 张卡；每张卡都显示来源到目标节点、`Axx` 输入 / 输出产物、交接说明、验收条件和候选入口，`WR14` 明确标成回流。
- 关系反查通过：从 `WR13` 的“想法谱系图”打开详情，标题、M13 当前工作位、固定来源、`WR13` / `WR14` 两条关系和候选边界均保持一致。
- 完整目录联合筛选通过：切换到 Agent 能力后选择 `M13 · 复盘并留下有效做法` + `可视化`，结果为 3 条同一 assignment 语境的能力，不出现研究主节点与角色的错误笛卡尔匹配。
- 关系与工作台数据一致：页面首屏显示 `4,184 + 96`、`149` 个节点工作位；本地 DOM 读取到 14 张 `.workflow-card`，桌面 `scrollWidth === innerWidth`。
- 约 388 CSS px 窄屏通过：关系详情 dialog 宽约 350px，左右边界均在视口内；页面无横向溢出，四角色与关系卡保持可读；浏览器控制台 `warning/error` 为 0。
- 本轮只验证静态关系渲染和筛选交互；没有把 24 条 v2 能力升级为安装、受控试跑、任务 benchmark 或生产可用。

### Round 7 — 三件事口语化

- 首选卡展开通过：统一显示“它能帮你做什么／用之前要准备什么／怎么确认它真的能用”，旧的“能帮什么／使用前要准备什么／怎样确认真能用”不再出现。
- 插件详情与 Agent 能力详情通过：两类弹窗使用完全相同的三条标题，内容仍来自各自的 `does / conditions / proof` 字段，没有改变研究结论。
- 浏览器控制台 `warning/error` 为 0；本轮未改变关系图、筛选逻辑或证据层级。

### Round 8 — 暖粉色 UI/UX 重做（文字方向回退）

- 视觉依据：产品 brief 与现有页面截图；新 ImageGen 目标因额度限制不可用，用户已授权文字方向回退。改版前截图为 `/tmp/codex-research-current.png`（JPEG，1910 × 1074，密度 1）；改版后桌面截图为 `/tmp/codex-research-warm-final.jpg`（JPEG，1910 × 1074，密度 1）；窄屏截图为 `/tmp/codex-research-warm-mobile-390.jpg`（文件 579 × 1254，浏览器 CSS 视口 388 × 840，约 1.5 倍像素密度，按 CSS 尺寸对照）。
- 桌面状态：默认工作台、M06、Codex 插件；页面背景、阶段轨道、节点页签、任务、交付结果和候选表均切换到暖粉色令牌，活动阶段和主按钮的状态对比清楚。整页 `scrollWidth === innerWidth`（1910 CSS px）。
- 交互状态：完整目录 → Agent 能力 → M13 → 可视化返回 3 条同一工作位结果；详情弹窗三条标题为“它能帮你做什么／用之前要准备什么／怎么确认它真的能用”；研究方法页渲染 14 张关系卡。
- 窄屏状态：约 388 CSS px 下阶段轨道和节点页签只在自身容器横向滚动，选中阶段自动滚入可见区；详情弹窗宽 350px，正文无横向溢出；`document.scrollWidth` 与 `body.scrollWidth` 均为 388。
- 五个保真面检查：字体沿用项目无外部资源的系统 sans/mono 回退；间距与圆角保持工作台节奏；颜色令牌改为暖粉色并分别覆盖深浅模式；无新增图片或图标资产，因此不存在图片质量漂移；页面文案与三件事口语化版本保持一致。
- 发现与处理：旧版的冷蓝/深灰和硬边框造成审计后台感，已改为玫瑰色背景、低对比边线、明确的活动状态、焦点环、悬停反馈和移动端选中项定位。没有发现 P0/P1/P2 可操作问题；未做伪造的视觉稿像素匹配。
- 控制台：浏览器 `warning/error` 读取结果为空；本轮没有新增外部资源、数据字段或运行时依赖。

## 最终判定

- P0：0
- P1：0
- P2：0 个未处理；图标差异为有意约束，不影响任务流。
- final result: passed
