# Design QA

## 目标

把网站从“研究成果展板”改为“全栈工作台地图”。用户无需先理解 M/A/G，就能从开发阶段进入具体节点，并在首屏看见任务、交付结果和 3 个候选。

视觉参考为本任务中用户明确选择的 ImageGen 方案 2，原始尺寸 1487 × 1058。实现截图使用 Codex 内置浏览器，桌面视口 1910 × 1075；对照时将实现截图居中裁成 1487 × 1058，与参考图并排检查。

## 验收记录

### Round 1

- P1：实现仍有一段巨型价值标题，工作台被压到第二屏。已把价值说明收进左栏，让阶段轨道和当前工作成为首要内容。
- P1：默认“写代码、跑测试”显示 YCloud、Ansight、Nightshift，虽然符合原始分数，但不够通用。已增加显式编辑层；完整排名不变，工作台结合通用性和当前安装状态选 3 个。
- P1：节点短句仍出现“独立证明它没有骗你”等生成式口号。已为 14 个节点重写自然语言标题、说明和三项任务。
- P2：卡片和证据文字偏小。已提高任务、候选说明、状态和依据文字字号。

### Round 2

- 桌面主路径通过：五阶段切换、节点页签、框架编号开关、任务勾选、推荐依据展开、节点目录跳转。
- 检索路径通过：全局搜索可带查询进入完整目录，筛选结果与详情弹窗可打开和关闭。
- 390 × 844 窄屏通过：导航换行、阶段横向滚动、节点页签横向滚动、任务单列，未出现横向撑破主内容。
- 参考图中的装饰性阶段图标没有照搬；实现使用数字阶段标记，避免伪造图标或引入外部运行资源。信息层级、布局、色彩和交互关系保持一致。

## 最终判定

- P0：0
- P1：0
- P2：0 个未处理；图标差异为有意约束，不影响任务流。
- final result: passed
